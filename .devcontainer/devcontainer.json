{
  "name": "purekrome.github.io",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "forwardPorts": [4321],
  "portsAttributes": {
    "4321": {
      "label": "Astro Dev Server",
      "onAutoForward": "notify"
    }
  },
  "mounts": [
    "source=${localWorkspaceFolderBasename}-node_modules,target=${containerWorkspaceFolder}/node_modules,type=volume,volume-opt=uid=1000,volume-opt=gid=1000"
  ],
  "postCreateCommand": "npm install",
  "postAttachCommand": "bash -lc 'if pgrep -f \"astro dev\" >/dev/null; then echo \"Astro dev already running\"; else npm run dev -- --host 0.0.0.0; fi'",
  "customizations": {
    "vscode": {
      "extensions": [
        "astro-build.astro-vscode",
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint"
      ]
    }
  },
  "remoteUser": "node"
}
